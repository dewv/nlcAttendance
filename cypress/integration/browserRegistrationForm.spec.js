describe("The browser registration form", () => {
    it("should allow user to define a location cookie" /*, () => {
        const username = `visitIndexPage${(new Date()).valueOf()}`;
        cy.loginStaff(username);
    } */ );
});
