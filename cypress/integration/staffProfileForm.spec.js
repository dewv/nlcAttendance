describe("The staff profile form", () => {
    it("should have SLP Instructor status 'No' for first time login" /*, () => {
        const username = `staffProfileForm${(new Date()).valueOf()}`;
        cy.loginStaff(username);
    } */ );

    it("should allow a staff member to change their SLP Instructor status");
});
